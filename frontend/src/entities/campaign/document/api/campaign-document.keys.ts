/**
 * Campaign Document Query Keys
 * @module entities/campaign-document/api
 *
 * Uses createChildEntityKeys factory to eliminate duplication.
 */

import { createChildEntityKeys } from '@shared/lib';

export const campaignDocumentKeys = createChildEntityKeys('campaign-documents');
